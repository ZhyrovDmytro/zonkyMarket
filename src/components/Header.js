import React from 'react';

const Header = () => (
    <header className="header">
        <img
          className="header__img"
          src={require('../pic/Zonky.png')}
          alt="Cook Book"
        />
    </header>
)

export default Header;